{
  "name": "drivesphere",
  "version": "1.0.0",
  "private": true,
  "description": "DriveSphere — Intelligent, hazard-aware, real-time dynamic route planning dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node start.js",
    "start-node": "node server.js",
    "start-python": "python server/app.py",
    "test": "node --check server.js && node --check frontend/app.js && node test_runner.js"
  },
  "engines": {
    "node": ">=18"
  }
}
